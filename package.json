{
  "name": "pagestate",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace @pagestate/ir",
    "build:schema": "npm run build:schema --workspace @pagestate/ir"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
