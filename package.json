{
  "name": "pagestate",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run build --workspace @pagestate/ir && npm run test --workspace @pagestate/ir && npm run test --workspace @pagestate/commands",
    "build": "npm run build --workspace @pagestate/ir && npm run build --workspace @pagestate/commands",
    "build:schema": "npm run build:schema --workspace @pagestate/ir && npm run build:schema --workspace @pagestate/commands"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
